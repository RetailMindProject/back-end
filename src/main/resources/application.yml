server.port: 8081
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pos_system_db
    username: postgres
    password: mypw2311222789@
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
